flask==2.3.3
flask-limiter==3.5.0
flask-sqlalchemy==3.0.5
werkzeug==2.3.7
requests==2.31.0
# BUG-13 FIX : chromadb==0.4.22 date de juillet 2023, incompatible avec
# Python 3.11+ et les dépendances récentes. On épingle >=0.5.0.
chromadb>=0.5.0
tiktoken==0.5.1
python-dotenv==1.0.0
# BUG-12 FIX : watchdog était utilisé dans workspace/watcher.py mais absent
# des dépendances → ImportError au runtime si le watcher est activé.
watchdog==3.0.0